import { MessengerClient } from 'messaging-api-messenger';
import SessionStore from '../session/SessionStore';
import Bot from './Bot';
import { MessengerRequestBody } from './MessengerConnector';
export default class MessengerBot extends Bot<MessengerRequestBody, MessengerClient> {
    constructor({ accessToken, appId, appSecret, sessionStore, sync, mapPageToAccessToken, verifyToken, batchConfig, origin, skipAppSecretProof, skipProfile, }: {
        accessToken: string;
        appId: string;
        appSecret: string;
        sessionStore: SessionStore;
        sync?: boolean;
        mapPageToAccessToken?: (pageId: string) => Promise<string>;
        verifyToken?: string;
        batchConfig?: Record<string, any>;
        origin?: string;
        skipAppSecretProof?: boolean;
        skipProfile?: boolean;
    });
}
//# sourceMappingURL=MessengerBot.d.ts.map