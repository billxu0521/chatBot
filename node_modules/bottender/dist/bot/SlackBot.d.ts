import { SlackOAuthClient } from 'messaging-api-slack';
import SessionStore from '../session/SessionStore';
import Bot from './Bot';
import { SlackRequestBody } from './SlackConnector';
export default class SlackBot extends Bot<SlackRequestBody, SlackOAuthClient> {
    _accessToken: string;
    constructor({ accessToken, sessionStore, sync, verificationToken, origin, skipProfile, }: {
        accessToken: string;
        sessionStore: SessionStore;
        sync?: boolean;
        verificationToken?: string;
        origin?: string;
        skipProfile?: boolean | null;
    });
    createRtmRuntime(): void;
}
//# sourceMappingURL=SlackBot.d.ts.map